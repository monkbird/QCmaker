import { useWizard } from '../context/WizardContext'
export const routeForState=(path:string,state:ReturnType<typeof useWizard>['state'])=>{if(path==='/config')return null;if(state.configStatus!=='ready')return'/config';if(path==='/topic')return null;if(!state.topic)return'/topic';if(path==='/data')return null;if(!state.dataRef||state.datasetRevision===null||!state.dataConfirmed)return'/data';if(path==='/discussion')return null;if(!state.discussionSummary)return'/discussion';if(path==='/visualization')return null;if(!state.chartOption||!state.chartImageRef)return'/visualization';return null}
